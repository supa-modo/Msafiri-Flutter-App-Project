import 'package:flutter/material.dart';

class Forgot_password_Phone extends StatelessWidget {
  const Forgot_password_Phone({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
